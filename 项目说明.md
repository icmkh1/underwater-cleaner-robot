# 智能水下清洁机器人控制系统 V1.0 · 项目说明

面向**水下清洁机器人（ROV）远程操控与监控**的 PySide2 上位机软件。上位机只与**树莓派 91 主控节点**建立连接，通过同一 TCP 通道同时完成：

- **视频**：接收主控摄像头 JPEG 帧（`4 字节大端长度 + JPEG`），实时显示；
- **指令**：下发 7 字段 ASCII 控制指令（推进器/舵机/刷子/灯光）；
- **监测**：显示连接、帧率、指令数、运行时长与各功能面板状态。

> 控制严格对齐软著《智能水下清洁机器人控制系统》：单主控连接、7 字段指令、参数限幅。

---

## 一、系统架构

```
        ┌───────────────┐   TCP 192.168.1.91:12345（指令 + 视频 + 状态）
        │  上位机(本软件) │───────────────────────────────────────────────┐
        │  PySide2 大屏  │   TCP 192.168.1.91:12346（CAM2 第二路视频，仅收流）│
        └───────────────┘───────────────────────────────────────────────┤
                                                                        ▼
                                                          ┌───────────────────────┐
                                                          │ 树莓派 91 主控节点      │
                                                          │ · 摄像头(JPEG 帧流)     │
                                                          │ · 推进器/舵机/刷子/灯光 │
                                                          │ · 灯光从控(转发)        │
                                                          └───────────────────────┘
```

| 通道 | 地址 | 说明 |
|---|---|---|
| 主通道 CAM1 + 指令 | `192.168.1.91:12345` | 视频帧流 + 7 字段指令（双向） |
| 第二路 CAM2 | `192.168.1.91:12346` | 第二路摄像头视频，**仅收流**（可隐藏，见「双摄像头」） |

> 上位机默认自动连接主控；也可在界面左侧「主控连接」处修改地址/端口后手动连接。

---

## 二、运行环境与安装

| 项目 | 版本/要求 |
|---|---|
| Python | 3.9（推荐） |
| GUI | `PySide2==5.15.2.1` |
| 图表 | `pyqtgraph>=0.13` |
| 图像 | `opencv-python-headless==4.10.0.84` |
| 数值 | `numpy<2` |
| 操作系统 | Windows（开发/演示环境） |

安装（二选一）：

```
pip install -r requirements.txt
```
或双击 **`安装依赖.bat`**（自动创建 `.venv` 并安装依赖）。

---

## 三、启动方式

| 方式 | 操作 |
|---|---|
| 推荐 | 双击 **`启动上位机.bat`** |
| 命令行 | `cd 项目目录 && ..\.venv39\Scripts\python.exe main.py` |
| 无真机演示 | 先双击 `启动模拟91服务器.bat`（或 `python mock_pi91_server.py`），再启动上位机 |

启动后**自动连接主控**（`config.json → robot91`，默认 `192.168.1.91:12345`），并顺带尝试连接 CAM2。

---

## 四、指令协议（7 字段）

```
MODE ANGLE d1,d2 s1,s2 BRUSH_STATE BRUSH_SPEED LIGHT
例：10 90 1,-1 180,180 10100 60 80
    00 90 0,0 0,0 00000 0 0      （停止/待机）
    0000                          （断开连接）
```

| 字段 | 含义 | 范围 |
|---|---|---|
| `MODE` | `10`=启动运行（采集+执行）/ `00`=停止待机 | 10 / 00 |
| `ANGLE` | 舵机角度 | 0~180 |
| `d1,d2` | 左右推进器方向（正转/反转/停止） | -1 / 0 / 1 |
| `s1,s2` | 左右推进器 PWM | 0~255 |
| `BRUSH_STATE` | **5 位** 01 字符串：小刷×4 + 大刷×1 | 00000~11111 |
| `BRUSH_SPEED` | 刷子转速百分比 | 0~100 |
| `LIGHT` | 灯光亮度百分比 | 0~100 |

- 参数在 `robot_protocol.py` 中统一**限幅**；非法字符按 0。
- **急停**：`00 90 0,0 0,0 00000 0 0`（全系统安全停机）。
- 兼容 6 字段固件：`robot_protocol.cmd_to_9_12_style()` 可裁掉灯字段。

> ⚠️ 刷子字段已由 3 位扩为 **5 位（4 小刷 + 1 大刷）**，真机固件需同步按 5 位解析。

---

## 五、功能说明（共 10 个页面）

| # | 页面 | 主要功能 |
|---|---|---|
| 0 | **首页概览** | 顶部紧凑状态条；**CAM1/CAM2 等大并排**画面；设备状态（左图右文）；运动控制（方向 + 推进器级数/电机组合 + 多灯/刷子）；任务信息；传感器数据；推进器输出（**正转/反转/停止** + PWM）；**磁轮控制** |
| 1 | **实时监控** | CAM1 左 / CAM2 右双画面 + OSD（深度/航向/俯仰/横滚/水温） |
| 2 | **任务规划** | 顶部**仿真界面**（北区湖/仿真2 地图）；深色**航点列表**（可编辑）；**路径预览**（轨迹图） |
| 3 | **自主控制** | 参数与控制（模式/深度/航向/速度/推力上限 + 运行状态 + 暂停/急停）；摄像头；**运动控制**（方向 + 启动/急停）；**磁轮控制**；机器数据 |
| 4 | **系统设置** | 主控连接（IP/端口/自动重连）；推进器安装；**视频与数据**（CAM1/CAM2 各自 端口/帧率/画质）；**配置导出/导入**；系统信息 |
| 5 | **数据管理** | **指令/遥测页签分表**（遥测 **11 字段**）；记录开关；**搜索**；**清空**；**导出双 CSV（UTF-8 BOM）**；运行统计 |
| 6 | **日志信息** | 分级（正常/告警/严重）筛选；导出日志 |
| 7 | **声纳探测** | 探测设置（量程/增益/频率/开关）；目标状态/数量；回波图；目标列表 |
| 8 | **历史曲线** | 电压/电量/舱温/湿度实时趋势（大图 + 图例） |
| 9 | **系统自检** | 一键运行协议单测 + 91 节点收发回路自检，显示 PASS/FAIL |

### 其他能力
- **双摄像头**：CAM1（主通道）/ CAM2（`12346`，仅收流）；首页等大并排、实时监控左右双画面；**CAM2 可隐藏**。
- **推进器级数 / 电机组合**：级数（低/中/高）缩放 PWM 输出；组合（双推正转/差速转向/镜像转向）决定直行与转弯映射。
- **多灯控制**：灯1/灯2/灯3 开关 + 共用亮度。
- **刷子控制**：小刷1~4 + 大刷（五路）+ 刷速。
- **磁轮控制**：自主调控（四轮磁力只读 + 工作状态分级）/ 手动介入（四轮独立 + 总磁力主滑块）；封装为可复用 `MagnetPanel`。
- **报警系统**：电压/电量/舱温/水温/电机温度/湿度阈值告警；顶部状态条 ⚠ 提示 + 告警流。
- **键盘操控**：`W/↑` 前进、`S/↓` 后退、`A/←` 左转、`D/→` 右转、`空格` 停止。
- **数据持久化**：连接地址、推进器安装、视频参数写入 `config.json`；支持导出/导入配置文件。

---

## 六、数据说明（真实 / 待接入）

| 数据 | 来源 |
|---|---|
| 连接、视频、指令下发 | **真实**（与 91 主控交互） |
| 深度/航向/姿态/水温/舱内环境、磁力计等遥测 | 目前**无下位机上报源**，界面显示 0 / 待接入；接入后替换 `SimTelemetry` 并从协议解析即可 |

> 默认「归零/中性」；如需系统状态随演示跳动，可在旧版体验开关处启用（当前默认关）。

---

## 七、文件结构

```
main.py                 上位机入口（执行 dashboard.main()）
dashboard.py            主界面与全部交互逻辑（10 页大屏 + 通信 + 控制）
robot_protocol.py       指令构造/解析/校验/限幅（GUI 与模拟器共用）
video_receiver.py       视频帧接收线程（4 字节长度 + JPEG，公共组件）
config_manager.py       配置读写（robot91/system）
logger.py               日志工具
mock_pi91_server.py     树莓派91 主控节点【本地模拟服务器】（合成视频帧 + 指令解析）
p91_cam_server.py       树莓派91【真机摄像头推送服务】（真实摄像头 → JPEG 帧流）
p91_cam_start.sh        树莓派端后台启动脚本（nohup + setsid，脱离 SSH 存活）
self_check.py           工程自检：协议单测 + 模拟 91 回路（+ 可选离屏 GUI 冒烟）
underwater_window.py    旧三页版主窗口（对软著参考 / 离屏冒烟，非正式入口）
robot_contrl.py         .ui 运行期加载器；motor_contrl.ui/.qrc/Qt 资源
assets/                 图标（assets/icons/*.svg）、地图资源（beiqu_lake/sim2/track_plot.png）等
config.json             本机运行配置（git 忽略；首次运行自动生成）
config.example.json     配置模板（提交到仓库，供他人复制为 config.json）
requirements.txt        运行依赖清单
安装依赖.bat / 启动上位机.bat / 启动模拟91服务器.bat    一键脚本（GBK+CRLF）
预览_*.png              各页面界面预览图
分支审查与整合报告.md    各 fork/PR 的审查与整合结论
```

---

## 八、质量自检

无需显示器、无需真机：

```
python self_check.py         # 协议单测 + 模拟 91 节点收发回路
python self_check.py --gui   # 追加离屏 GUI 冒烟
```

退出码 0 = 全部通过。GUI 侧另做过**离屏几何审计**（多分辨率 × 全页面，检查文字裁切/零尺寸控件）。

---

## 九、无真机联调（本地模拟）

1. 双击 `启动模拟91服务器.bat` —— 监听 `127.0.0.1:12345`，持续下发合成视频帧，并解析指令；
2. （如需演示第二路）再执行 `python mock_pi91_server.py --port 12346`；
3. 双击 `启动上位机.bat`，在「主控连接」填 `127.0.0.1` + `12345`，点「连 接」。

## 十、真机联调（树莓派 91）

1. 上位机与树莓派同网段（建议电脑固定 IP，如 `192.168.1.99`，掩码 `255.255.255.0`）；
2. 树莓派上运行摄像头推送服务（`p91_cam_server.py`，监听 `0.0.0.0:12345`）；
3. 上位机「主控连接」填 `192.168.1.91` + `12345`，点「连 接」，即显示真机摄像头画面；
4. 若另有第二路摄像头/流，监听 `12346` 即可在 CAM2 显示。

---

## 十一、协作与版本

- **入口唯一**：正式运行以 `main.py → dashboard.py`（单页大屏版）为准。
- **协议是契约**：修改通信格式须同步 `robot_protocol.py`、`mock_pi91_server.py` 与本文档，并跑 `python self_check.py`。
- **配置**：`.venv`、`logs/`、`data/`、`config.json` 已 git 忽略；复制 `config.example.json` 为 `config.json` 并按机器修改。
- **流程**：各人在独立分支开发，`git pull` 后再改，合回主分支走合并/评审；按「协议 / GUI / 节点 / 工具」划分职责。
