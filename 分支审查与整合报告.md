# 分支审查与整合报告

- 仓库：`https://github.com/icmkh1/underwater-cleaner-robot`
- 当前 `master`：`ee2226b`
- 报告时间：本轮分支审查与整合完成后

---

## 一、仓库协作概况

主仓库有 **4 个 fork**，共 **5 个 open PR**：

| 人员 | GitHub | 分支 | PR | 状态 |
|---|---|---|---|---|
| 梁展翔 | `Hendry-sude` | `Hendry-update` | #2「上位机软件界面优化」 | open，基于旧基 |
| 梁展翔 | `Hendry-sude` | `feature/magnet-panel-enhance` | #5「磁轮控制」 | open，**基于最新 master** |
| 李枫浩 | `Eddie-534` | `ui-data-refine` | #1「UI 精修与双摄像头支持」 | open，基于旧基 |
| ljzzzzzzzzzzzz | `ljzzzzzzzzzzzz` | `ui_data_fix` | #4「系统设置ui优化」 | open，基于旧基 |
| huaxie602 | `huaxie602` | `refine-data-ui` | （单提交） | 基于旧基 |

> 关键点：除 **PR#5** 外，其余分支都基于较旧的 `818768e`，落后当前 master 十余个提交，直接合入会**大量冲突且大量重复**。

---

## 二、各分支审查结论

### ✅ PR#5「磁轮控制」（Hendry-sude）—— 已整合
- 唯一基于最新 master 的分支，可无冲突合入。
- 内容：首页新增「磁轮控制」面板（自主/手动调控 + 四轮磁力 + 工作状态分级）。
- **已整合**：并进一步封装为**可复用 `MagnetPanel`**，首页与自主控制各放一个实例。

### ⚠️ PR#2「界面优化」（Hendry-sude）—— 未整板块，思路已吸收
- 内容：整体收紧（面板内距/页边距/间距）、字号放大、系统状态面板撑满、深度指示刻度加密。
- **结论**：核心已被当前 master 实现/吸收（首页紧凑度、深度指示每 5 刻度 + 贴底等）。直接合入会冲突。

### ⚠️ PR#1「UI 精修 + 双摄像头」（李枫浩）—— 任务规划已整合，**双摄为本项目自研**
- 内容：仿真界面（北区湖/仿真2 静态图）、路径预览静态轨迹图、三表格深色化、设备状态左图右文、双摄像头。
- **已整合**：**任务规划页完全按其版本**；并**下载其真实地图资源**（`beiqu_lake/sim2/track_plot.png`）。
- **注意**：其"路径预览"把**可交互实时曲线换成了静态图**（编辑航点不再更新路径），且基于旧基会**丢失本项目已有的任务配置/开始暂停停止/进度**——这是"完全替换"的代价。
- **双摄像头**：本项目已在 master 自研实现（CAM1/CAM2 独立通道 12346、首页等大并排、实时监控双画面）。

### ⚠️ PR#4「系统设置ui优化」（ljzzzzzzzzzzzz）—— 布局思路已整合
- 内容：设置页两模块**竖向堆叠、全宽**（`v_layout`），grid 间距调整；另有一行多余的 `os.environ['PYQTGRAPH_QT_LIB']`（冗余）。
- **已整合**：取其"竖向堆叠结构"，但按"紧凑"要求把间距调紧（14），系统信息在下。

### ✅ huaxie602「细化数据管理」—— 已整合
- 内容：指令/遥测**拆页签分表**、遥测扩 **11 字段**、**导出指令+遥测双 CSV（UTF-8 BOM）**、清空记录、运行统计、急停入库；界面字号/紧凑/高分屏。
- **已整合**：页签式数据管理 + 11 字段遥测 + 双 CSV + 急停入库；`_tick_ui` 采集扩到 11 字段；**保留**本项目的搜索/清空。
- **未采纳**：界面放大/高分屏/`showMaximized()`（master 已有字号与高分屏），仅保留其数据管理部分。

---

## 三、已整合进 master 的内容（对应提交）

| 提交 | 内容 |
|---|---|
| `ee2226b` | 数据管理按 huaxie602：指令/遥测页签 + 遥测 11 字段 + 双 CSV(BOM) + 搜索/清空 + 急停入库 |
| `6a90798` | 系统设置按 ljzzzzzzzzzzzz：两模块竖向堆叠全宽、紧凑间距 |
| `07dbb2f` | 自主控制页加**运动控制**模块 + **磁轮控制**模块（可复用 `MagnetPanel`） |
| `b056b63` | 任务规划**完全按李枫浩 PR#1**：仿真界面 + 深色航点表 + 静态轨迹图 |
| `83e1ea2` | 替换为李枫浩真实地图资源（beiqu_lake/sim2/track_plot） |
| `bea19f6` | 合入 PR#5 磁轮控制（首页磁轮面板 + 底部四卡） |
| `e5495d4` | 首页按梁展翔紧凑度：底部三卡 Expanding、侧栏系统状态撑满大字、导航 17px |
| `6a90798` 之前 | 5 位刷子协议（4 小 + 1 大）、推进器级数/电机组合、多灯控制、深度指示融合、字体放大等 |

---

## 四、未整合 / 建议

1. **PR#1 / PR#2 / PR#4**：均基于旧基 `818768e`，且核心已被 master 实现或替代 —— **建议让作者 rebase 到最新 master 后再投**，否则合入会冲突 + 功能回退。
2. **PR#5**：已整合，可在 GitHub 上**标记为已合入/关闭**。
3. **可选的兼得方案**：李枫浩任务规划（仿真图 + 静态轨迹）与本项目的"动态路径 + 执行控制"目前二选一；若要兼得，可把动态路径**叠加在轨迹底图**上、并把任务配置/开始暂停停止加回。
4. **协议提醒**：本项目刷子字段已扩为 **5 位**（`BRUSH_STATE` 小刷×4 + 大刷×1），真机固件需同步；各 fork 均基于 3 位旧格式。

---

## 五、当前功能总览（master）

- **首页概览**：顶部状态条、CAM1/CAM2 等大并排、设备状态(左图右文)、运动控制(级数/组合/多灯/刷子)、任务信息、传感器数据、推进器输出(方向 正转/反转/停止 + PWM)、**磁轮控制**。
- **实时监控**：CAM1/CAM2 双画面 + OSD。
- **任务规划**：仿真界面(北区湖/仿真2) + 深色航点表 + 路径预览(静态轨迹图)。
- **自主控制**：参数与控制 + 摄像头 + **运动控制** + **磁轮控制** + 机器数据。
- **数据管理**：指令/遥测**页签**分表(遥测 11 字段)、搜索、清空、双 CSV 导出、运行统计。
- **日志信息**：分级筛选 + 导出。
- **声纳探测**：探测设置(量程/增益/频率/开关) + 目标列表 + 回波图。
- **历史曲线**：电压/电量/舱温/湿度实时趋势。
- **系统自检**：复用 `self_check.py`，协议 + 回路自检。
- **系统设置**：主控连接 + 视频与数据(竖向堆叠) + 系统信息；配置导入/导出。
- **其他**：键盘操控(WASD/空格)、报警系统(阈值)、启动自动连接主控、双摄像头(12346)。

---

## 六、验证方式

- 离屏几何审计（1600×1048 / 1280×720 / 1920×1080，10 页全遍历）：**0 裁切、0 零尺寸**。
- 关键交互冒烟：任务/自主/数据/声纳/磁轮/急停/搜索/清空等均通过。
- 协议自检：`python self_check.py` 全部 PASS。
